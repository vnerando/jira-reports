# Ignorar variáveis de ambiente sensíveis (Tokens do Jira e Contas de Serviço)
credentials/

# Ignorar pacotes e dependências (se houver no futuro)
node_modules/
package-lock.json

# Ignorar relatórios JSON pesados extraídos dinamicamente
raw_data/
issues_by_type/
